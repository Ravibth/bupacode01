// Use the HTTPS development URL to avoid CORS issues caused by the HTTP -> HTTPS redirect.
// Make sure the ASP.NET backend is running with the "https" launch profile (e.g. https://localhost:7062).
export const API_BASE = 'https://localhost:7062/api';